// Copy this file to firebase-config.js after creating the Firebase web app.
// The Firebase config values are safe to ship in frontend code. Admin access is
// protected by Firebase Auth plus Firestore Security Rules, not by hiding config.
window.NEW_EDEN_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
